name = "policy-gateway-recovery"
main = "index.js"
compatibility_date = "2025-07-01"

# KV 命名空间（需手动创建）
# wrangler kv:namespace create AUTH_TABLE
[[kv_namespaces]]
binding = "AUTH_TABLE"
id = ""

# 环境变量（需手动设置）
# wrangler secret put RECOVERY_TOKEN
[vars]
RECOVERY_TOKEN = ""
