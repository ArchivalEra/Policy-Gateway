[package]
name = "policy-gateway-vm"
version = "0.3.3"
edition = "2021"

# VM — "不死鸟" 版本管理器
# 独立于 policy-gateway 之外。主程序崩了它还能回滚。
# 闪存占用目标: ~80kB (UPX后)

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
ring = "0.17"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
strip = true
codegen-units = 1
